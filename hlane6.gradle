apply plugin: 'java'

version = '1.0'

defaultTasks 'jar'

jar {
    manifest {
        attributes 'Implementation-Title': 'CS 2340 MULE M2', 'Implementation-Version': version
    }
}

dependencies {
    compile files('lib/resources.jar')
}
